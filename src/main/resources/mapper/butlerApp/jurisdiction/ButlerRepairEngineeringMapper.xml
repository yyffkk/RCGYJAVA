<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.api.butlerApp.dao.jurisdiction.ButlerRepairEngineeringDao">

    <select id="list" parameterType="com.api.model.butlerApp.ButlerRepairEngineeringSearch" resultType="com.api.vo.butlerApp.ButlerRepairVo">
        select * from sys_report_repair_engineering
    </select>

    <insert id="insert" parameterType="com.api.model.butlerApp.ButlerRepairEngineering">
        insert into sys_report_repair_engineering(
            repair_area,
            code,
            type,
            report_detail,
            status,
            create_id,
            create_date
        )values (
            #{repairArea},
            #{code},
            #{type},
            #{reportDetail},
            #{status},
            #{createId},
            #{createDate}
        )
        <selectKey keyProperty="id" resultType="java.lang.Integer">
            select LAST_INSERT_ID() AS id
        </selectKey>
    </insert>

</mapper>